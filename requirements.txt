# Direct dependencies only — pip resolves transitives.
# Regenerate the full uv export with: uv export --no-hashes --no-dev > requirements.lock
# (but the slim hand-curated version below is preferred for Vercel — fewer parsing surprises.)

anthropic>=0.104.1
fastapi>=0.136.1
httpx>=0.28.1
mcp>=1.27.1
openai>=2.38.0
pinecone>=9.0.1
pydantic>=2.13.4
python-dotenv>=1.2.2
supabase>=2.30.0
uvicorn[standard]>=0.47.0
